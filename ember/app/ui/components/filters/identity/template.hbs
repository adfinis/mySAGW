<Filters::Dropdown
  @label={{t "documents.filters.identity"}}
  @selected={{this.selectedIdentityOptions}}
  @options={{this.searchedIdentites}}
  @onChange={{this.onChange}}
  @onInput={{this.updateIdentitySearch}}
/>
