mutation ($form: ID!, $workflow: ID!) {
  startCase(input: { form: $form, workflow: $workflow }) {
    case {
      id
      workItems {
        edges {
          node {
            id
          }
        }
      }
    }
  }
}
