query GetRootForms($slug: String, $isPublished: Boolean, $isArchived: Boolean) {
  allForms(
    orderBy: NAME_ASC,
    slug: $slug,
    isPublished: $isPublished,
    isArchived: $isArchived
  ) {
    edges {
      node {
        slug
        name
        meta
      }
    }
  }
}
