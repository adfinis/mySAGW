query getWorkItems($filter: [WorkItemFilterSetType]) {
  allWorkItems(filter: $filter) {
    edges {
      id
      status
      meta
      addressedGroups
      controllingGroups
      assignedUsers
      name
      task {
        slug
      }
    }
  }
}