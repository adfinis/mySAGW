query getForms {
  allForms(
    filter: [
      { questions: ["dossier-nr"] }
      { isPublished: true }
      { isArchived: false }
    ]
  ) {
    edges {
      node {
        slug
        name
        meta
      }
    }
  }
}
